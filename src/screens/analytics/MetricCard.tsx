import React from 'react';
import { Text, View } from 'react-native';
export function MetricCard({ label, value }: { label: string; value: string | number }) { return <View style={{ flex: 1, minWidth: 140, padding: 14, borderWidth: 1, borderColor: '#e5e7eb', borderRadius: 12, margin: 5, backgroundColor: '#fff' }}><Text style={{ fontSize: 25, fontWeight: '800' }}>{value}</Text><Text style={{ color: '#6b7280', marginTop: 4 }}>{label}</Text></View>; }
export function BarChart({ items }: { items: Array<{ label: string; value: number }> }) { const max = Math.max(1, ...items.map(x => x.value)); return <View style={{ marginTop: 8 }}>{items.slice(0, 8).map(item => <View key={item.label} style={{ marginBottom: 10 }}><Text style={{ fontSize: 12, marginBottom: 4 }}>{item.label} · {item.value}</Text><View style={{ height: 10, backgroundColor: '#e5e7eb', borderRadius: 5, overflow: 'hidden' }}><View style={{ height: 10, width: `${Math.max(2, (item.value / max) * 100)}%`, backgroundColor: '#111827' }} /></View></View>)}</View>; }
