/**
 * Firebase email-link configuration is derived at runtime from the configured
 * Firebase project ID. No Firebase Dynamic Links domain is used.
 *
 * The link target is:
 *   https://<project-id>.firebaseapp.com/alumni-finish
 *
 * Ensure that the project Hosting domain is enabled and that the Android/iOS
 * applications are configured for that Firebase Hosting domain.
 */
export const FIREBASE_LINK_PATH = '/alumni-finish';
export const FIREBASE_FUNCTIONS_REGION = 'africa-south1';
